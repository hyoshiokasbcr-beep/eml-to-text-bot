[build]
  functions = "netlify/functions"
  # あなたの環境で成功する方を設定（npm ci で失敗するなら npm install）
  command   = "npm install"

[functions]
  node_bundler = "esbuild"
  external_node_modules = [
    "@netlify/blobs",
    "mailparser",
    "html-to-text",
    "msgreader",
    "node-fetch"
  ]

[[headers]]
  for = "/.netlify/functions/*"
  [headers.values]
  Cache-Control = "no-store"
